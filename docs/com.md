# Comunicados del propietario

Los temas pendientes de revisión se consolidaron en `00-pendientes.md` (único fichero de pendientes). Los dos puntos históricos de este archivo están **resueltos y verificados**:

1. ✅ Formularios sin `alert()`/`prompt()` nativos — reemplazados por componentes personalizados (`form-modal`, `Utils.confirm`). No quedan llamadas nativas en `src/frontend/js`.
2. ✅ Desglose por prioridades y % gastos proyectado vs real — implementado en cierre de turno, ficha de turno (`ventas.js`) y cierre de mes (`contabilidad.js`).

Este archivo se mantiene por trazabilidad histórica; no es fuente de pendientes.